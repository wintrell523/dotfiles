# Zsh plugins managed by antidote
# See: https://getantidote.github.io/usage

# Prompt theme — load only the theme file, not the p9k compat shim
# p10k instant prompt is handled by the block at the top of .zshrc
romkatv/powerlevel10k path:powerlevel10k.zsh-theme

# Core plugins
zsh-users/zsh-autosuggestions
zsh-users/zsh-history-substring-search

# Utilities
djui/alias-tips
hlissner/zsh-autopair kind:defer

# Syntax highlighting — defer so it loads last
zsh-users/zsh-syntax-highlighting kind:defer
